export function ScoreBadge({ score, rank, total }: { score: number; rank?: number; total?: number }) { const color = score >= 8.5 ? 'text-emerald-400' : score >= 6.7 ? 'text-green-500' : score >= 3.4 ? 'text-amber-500' : 'text-zinc-500'; return <span className={`inline-flex items-center gap-2 font-semibold tabular-nums ${color}`}><span className="text-4xl tracking-tight">{score.toFixed(1)}</span>{rank && total && <span className="rounded-full border px-2 py-1 text-xs text-rally-secondary">#{rank} of {total}</span>}</span>; }
