# Seed venue photos

Add local JPEGs for every filename referenced in `scripts/data/photos.ts` here. For example, add
`basketball-01.jpg`, `basketball-02.jpg`, `pickleball-01.jpg`, and so on. These are deliberately
local paths so the demo is not dependent on a remote photo host.
