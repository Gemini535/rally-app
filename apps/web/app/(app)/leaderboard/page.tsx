'use client';
import { useQuery } from '@tanstack/react-query';
import { api } from '@/lib/api-client';
import { EmptyState } from '@/components/shared/empty-state';
type Row = { rank: number; venue: { id: string; name: string; neighborhood: string | null }; elo: number; nEntries: number; avgRallyScore: number | null };
export default function LeaderboardPage() { const query = useQuery({ queryKey: ['leaderboard'], queryFn: () => api.get<{ items: Row[] }>('/leaderboard?sport=basketball') }); return <main className="mx-auto max-w-2xl p-5"><p className="text-sm text-rally-secondary">CHICAGO · BASKETBALL</p><h1 className="mt-1 text-3xl font-semibold">Leaderboard</h1><p className="mt-2 text-sm text-rally-secondary">Ranked by head-to-head record across venue comparisons.</p><div className="mt-5 overflow-hidden rounded-card border border-rally-border bg-rally-surface">{query.isLoading ? Array.from({ length: 8 }, (_, index) => <div key={index} className="flex h-20 items-center gap-4 border-b border-rally-border p-4"><div className="h-6 w-7 animate-pulse rounded bg-rally-elevated" /><div className="flex-1 space-y-2"><div className="h-4 w-2/3 animate-pulse rounded bg-rally-elevated" /><div className="h-3 w-1/3 animate-pulse rounded bg-rally-elevated" /></div><div className="h-7 w-10 animate-pulse rounded bg-rally-elevated" /></div>) : query.data?.items.length ? query.data.items.map((row) => <article key={row.venue.id} className="flex items-center gap-4 border-b border-rally-border p-4 last:border-0"><strong className="w-7 text-xl tabular-nums text-rally-tertiary">{row.rank}</strong><div className="min-w-0 flex-1"><h2 className="truncate font-semibold">{row.venue.name}</h2><p className="text-sm text-rally-secondary">{row.venue.neighborhood ?? 'Chicago'} · {row.nEntries} rankings</p></div><div className="text-right"><p className="font-semibold tabular-nums">{row.elo.toFixed(0)}</p><p className="text-xs text-rally-tertiary">{row.avgRallyScore?.toFixed(1) ?? '—'} avg</p></div></article>) : <EmptyState icon="🏆" headline="No rankings yet" body="Rank a venue to start the city board." />}</div></main>; }
